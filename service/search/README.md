# Search service working with data in OSDU format

Search service accepting search requests in OSDU format, transforming them into DELFI compatible 
format and converting the result into OSDU format. 